
public class main {
    private static int k;
    //get input
    //find the length 
    //compare and swap if the character is small or big
    public static void main(String[]args ) {
        String str = "array";
    char arr[]=str.toCharArray();
    char temp;
        int i;
for (i=0;i<arr.length;i++){
        for (k=i+1;k<arr.length;j++);{
            if (arr[i]>arr[k]);{
                temp=arr[i];
                arr[i]=arr[k];
                arr[k]=temp;
            }
        }
      }
    }
System.out.println(new main(arr));
    }